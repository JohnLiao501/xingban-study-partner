# 阶段 3 关闭与阶段 4 启动计划

> 基线日期：2026-09-01
> 当前结论：阶段 3 尚未完成；真实捕获、内容保护、秘密存储与既有零残留证据有效，G3-A 隔离验收辅助已经实现并完成自动门禁。
> 执行原则：不重复已经通过的手工授权，不用真实外网或真实 API 密钥，不污染用户历史，不提前生产第三方 IP 素材。

## 1. 已关闭门禁

以下项目已有自动化或真实桌面证据，后续只在相关实现发生变化时重验：

| 门禁 | 当前证据 | 状态 |
| --- | --- | --- |
| 类型、测试与构建 | 29 个测试文件，198 项通过、1 项默认跳过；类型检查、生产构建与完整 G3-A 预检通过 | 已通过 |
| Windows 前台探针 | 自包含构建与真实 sidecar 专项 6 项通过，退出后进程为 0 | 已通过 |
| 显式屏幕授权 | 用户主动选源后流可建立、停止并安全退出 | 已通过 |
| 真实内容保护 | 768×432 单帧中原生对照窗可见，主窗与悬浮窗受保护标记不可见 | 已通过 |
| `safeStorage` | 临时假密钥保存、数据库重开解密、清除与再次重开通过 | 已通过 |
| 隐私复扫 | 应用数据、SQLite 与相关临时目录无巡查截图、BLOB 或 data-image 残留 | 已通过 |

内容保护验收中的彩色色块只在显式验收开关下出现，不属于正式 UI。外部摄像头可以拍到物理屏幕，因此手机录屏只用于确认诊断界面按时消失，不作为系统截屏保护的判据。

## 2. 关闭计划单元

| ID | 门禁 | 是否需要用户参与 | 完成证据 |
| --- | --- | --- | --- |
| G3-A | 隔离验收辅助 | 否 | 已完成自动门禁：临时 `userData`、严格本机 mock、确定性节点、低敏完整轨迹/数据库判据、全量流式扫描与精确清理均有测试；不把它算作真实桌面场 |
| G3-B | 真实 25 分钟人工场 | 是，一次 | 满 25 分钟的结构化事件、正确偏航/结算/信赖与退出后零残留 |
| G3-C | 全屏、系统停止共享与截图窗销毁 | 部分 | 全屏切换、系统终止流、隐藏截图工作窗销毁和恢复路径不崩溃、不补罚 |
| G3-D | 休眠/恢复 | 是 | 恢复为暂停或安全降级，计时不偷跑，流与探针可重建或明确失败 |
| G3-E | 125%/150% DPI | 是，需同意修改系统缩放 | 两档缩放下窗口、选源、单帧尺寸、坐标和悬浮窗均正常 |
| G3-F | 多显示器 | 需要真实第二显示器 | 每个显示器可独立选择，所选源正确，断开副屏后安全降级 |
| G3-G | 阶段关闭复验 | 否 | 全量命令、真实进程清理、隐私扫描和文档状态全部一致 |

当前机器只有单显示器和 100% DPI，因此 G3-F 不能在现有环境中宣称通过。G3-E 也不能未经用户同意擅自修改系统显示设置。
G3-A 已关闭；当前剩余门禁为 G3-B～G3-G。

2026-09-01 的 G3-B 尝试只证明 2:00 本地允许规则与非阻塞反馈路径；3:00 禁止规则没有形成连续 20 秒命中，最后一次重启又在会话前遇到 GPU/renderer `launch-failed`。无效运行均完成进程退出、隐私扫描和隔离目录精确清理。用户决定跳过剩余复合验收，因此本计划保持开放，不得据此启动阶段 4。

## 3. 已完成：隔离验收辅助

G3-A 已于 2026-08-31 完成，下一步安排真实 25 分钟场。该批次只增加验收基础设施与同场重新授权入口，不改变正式产品判定语义。

### 3.1 隔离用户数据

- 验收启动时使用操作系统临时目录下的独立 `userData`，不读取或写入用户正式 SQLite、设置、历史和信赖。
- 目录名使用随机后缀，启动前解析并验证路径位于系统临时目录内。
- 正常退出会等待截图 renderer 的媒体轨停止回执与 sidecar 退出；外层启动器在中断时先对本次 Electron 精确 PID 树执行有界清理，确认子进程退出后才读取证据和删除目录。无法确认退出时门禁失败且不对仍活动的目录执行递归删除。
- 不使用删除正式数据库、清空历史或覆盖迁移来实现隔离。

### 3.2 本机脚本化 mock AI

- 只监听 `127.0.0.1` 的随机端口，不访问真实外网，不要求用户真实 API 密钥。
- 默认 5 步原始响应依次是：AI `focused`、高置信 `distracted/private_communication`、两次高置信 `distracted/entertainment_content`、AI `focused`；私人通讯由产品政策层降级为 `uncertain`，不是 mock 直接伪造最终标签。
- 只接受固定本机假令牌、`stage3-local-mock` 模型和不含窗口标题的严格请求形状；额外请求、错位、脚本耗尽或任何形状违规都计入失败。
- 请求体只在内存中解析和计数，响应后立即释放；不记录 JPEG、base64、目标、窗口标题、Authorization 或模型原始请求。
- mock/适配器测试覆盖超时、429、5xx 和无效 JSON；政策层既有测试另行覆盖标签/原因码语义矛盾，以上失败均由编排层安全降级为 `uncertain`。

### 3.3 确定性巡查与摘要

- 只在验收开关下使用固定种子和明确的巡查节点，正式模式继续使用现有随机调度。
- 输出仅包含时间点、状态、标签、原因码、偏航计数、流状态和通过/失败布尔值。
- 摘要不得包含截图、base64、完整目标、完整窗口标题、密钥或模型请求体。
- 自动化证明超时/协议失败安全降级、任何中断结果不能通过、精确目录清理单元和正常退出回执逻辑；真实 runner 的 SIGINT/SIGTERM 媒体流/进程树清理仍须在 G3-B/G3-G 留下桌面证据。

### 3.4 实现与预检证据

- `pnpm run acceptance:stage3:preflight` 验证固定节点 2:00、3:00、5:00、8:00、11:00、15:00、18:00，严格消费恰好 5 个 mock 请求；对一份安全文本文件执行不限 8 MiB 的流式扫描，确认无图片、data-image、JPEG base64、固定假令牌或符号链接，再删除精确目录。
- `pnpm run acceptance:stage3` 使用外层 Node 启动器创建随机临时目录和回环 mock，再启动 Electron。启动器要求恰好 8 条有序观察、由 15 秒定时器产生且持久化时间差不少于 14.5 秒的娱乐二帧、`active → stopped → active`、暂停/恢复、7 个巡查、1500 秒结算、B/29 信赖只写一次、无 BLOB/截图/base64/假令牌残留及正常退出清理回执；未满 25 分钟、中断、额外 mock 请求或任一证据缺失均返回失败。
- 主界面在停止共享后提供“重新授权屏幕”：必须由用户再次点击加载、选择 source，再由主进程校验活动会话、一次性 token 和源仍存在；不会复用旧授权或自动选择第一个屏幕。
- 隔离 Electron 短时冒烟已确认手工触发预览后可见窗口数 `1 → 2 → 1`，第二次变化发生在 6 秒复查时；中断后项目进程和验收临时目录均为 0。该项只覆盖真实自动隐藏，不替代 G3-B。
- 自动测试与浏览器 QA 不能证明真实屏幕流、真实前台应用切换或 25 分钟墙钟运行，因此 G3-B 仍未完成。

## 4. 真实 25 分钟人工场

该场使用原创 demo 伙伴、隔离用户数据和本机 mock AI。用户只需完成一次选源和一次重新授权；Agent 负责启动、观察、计时、结束、进程检查和隐私扫描。下列分钟数均指有效专注计时，暂停耗时不计入 25 分钟，因此实际墙钟时间可以略长于 25 分钟。

中途巡查结果和置顶悬浮窗会在 5 秒后自动收起并继续计时，无需返回主窗点击继续；只有达到计划时长后的结算/休息选择保留人工操作。若任一中途反馈持续遮挡或计时未自动恢复，本场立即判失败并修复，不要求用户绕过。

建议顺序如下，具体节点由会话面板提示给出，不依赖用户记忆：

1. 0～2:10：让允许进程保持前台；2:00 节点应 `focused`，不截屏、不调用 AI。
2. 2:10～3:30：尽快切到禁止进程并保持至少 20 秒；3:00 节点只记一次本地规则偏航，提前量包含用户反应与 5 秒采样余量。
3. 3:30～7:30：切到不匹配规则的窗口；5:00 节点由 mock AI 返回 `focused`。
4. 7:30～10:30：保持未知窗口；8:00 附近可能先出现五分钟恢复反馈，自动回流后私人通讯节点必须为 `uncertain` 且不扣偏航，低敏轨迹允许该节点延后至 8:05 内。
5. 10:30～14:00：保持同一未知窗口；11:00 首帧与至少 15 秒后的新帧都为高置信娱乐分心，只有第二帧确认后记一次 AI 偏航。
6. 14:00～16:00：从托盘或会话面板停止共享；15:00 节点只能安全降级，不产生迟到处罚。
7. 16:00～18:00：点击“重新授权屏幕”，再次加载并明确选择同一 source；随后暂停和恢复一次，暂停时间不计入专注时长。
8. 18:00～25:00：让允许进程保持前台；18:00 节点应 `focused`，随后完成会话并确认结算、评价、历史和伙伴信赖只写入一次。

通过条件：恰好两次预期偏航，其中一次来自连续本地禁止规则、一次来自 AI 两帧确认；全部 `uncertain` 不处罚；停止共享后无新帧；暂停计时正确；正常结算幂等；退出后 Electron、sidecar、mock server 和捕获流均为 0；临时验收目录删除前扫描无截图/base64，删除后目录不存在。

若应用随机时机、系统弹窗或人为操作使某个场景没有实际发生，本场记录为未完成而不是猜测通过。只补做缺失场景，不重复已取得的内容保护证据。

## 5. Windows 环境矩阵

G3-C～G3-F 分开记录，不能用单次 25 分钟场笼统代替：

| 场景 | 最低检查 |
| --- | --- |
| 全屏应用 | 主窗隐藏/恢复、悬浮窗置顶、所选屏幕源不变、取帧不含应用窗口 |
| 系统停止共享 | 收到 stream ended、AI 取消、会话继续、本地规则仍可用、无迟到处罚 |
| 截图工作窗销毁 | 当前流终止、待确认 AI 取消、其他窗口不获得截图权限、会话安全降级 |
| 休眠/恢复 | 暂停期间不累计时间；恢复后探针和捕获可重建，失败则明确降级 |
| 125%/150% DPI | 主窗/弹窗无裁切，屏幕源与物理显示器一致，768 px 上限仍成立 |
| 多显示器 | 每个 `screen:` source 可识别；选择、拔出副屏和重新授权均安全 |

每个单元记录日期、Windows 版本、显示器数量、缩放、实际结果、残留进程和隐私扫描。没有对应硬件时标记 `blocked-by-environment`，不得标记 `passed`，也不因此重写产品规则。

## 6. 后续执行批次与安排

本批用户已决定停止继续复合验收，因此下面只作为后续排期，不在本次提交中把任何未执行项标记为通过。后续按 B0～B5 顺序推进；前一批退出条件未满足时不得进入后一批，也不通过关闭 GPU、缩短连续判定时间或修改固定节点来换取表面通过。

| 批次 | 内容 | 用户参与 | 预计占用 | 进入条件 | 退出条件 |
| --- | --- | --- | --- | --- | --- |
| B0 | 启动稳定性与 GPU `launch-failed` 诊断 | 否 | 20～40 分钟 | 当前自动门禁全绿、无残留进程 | 连续 3 次隔离冷启动均能到达设置界面；失败启动也能退出并清除临时目录；若仍失败，形成含退出码和运行时依赖的根因记录 |
| B1 | 0～3:10 节点预演，只验证前台切换节奏 | 是，约 4 分钟 | 5～10 分钟 | B0 通过 | 2:00 为 `allowed_app / local-rule`；用户在 2:10 后手动切到禁止进程并保持，3:00 为 `blocked_app / local-rule`；该预演不算 G3-B |
| B2 | 完整 G3-B 25 分钟单场 | 是，约 30 分钟 | 30～40 分钟 | B1 通过且同日环境未变化 | 8 条轨迹、两次预期偏航、B/29 信赖、捕获重启、暂停恢复、正常退出和零残留全部通过 |
| B3 | G3-C 与 G3-D：全屏、系统停止、截图窗销毁、休眠恢复 | 部分 | 20～30 分钟 | B2 通过 | 各场景分别留存低敏结果、进程清理和隐私扫描；任何迟到处罚均为失败 |
| B4 | G3-E 与 G3-F：DPI、多显示器 | 是，且取决于授权/硬件 | 每档约 15 分钟 | 用户同意修改缩放或真实第二显示器可用 | 125%/150% 分别通过；多屏只有真实硬件证据才通过，否则保持 `blocked-by-environment` |
| B5 | G3-G 最终关闭或阻塞收口 | 否 | 15～25 分钟 | B0～B4 均有明确通过或环境阻塞结论 | 全量命令、真实 probe、进程/流/临时目录、隐私扫描和文档状态一致；只有 B0～B4 全部通过才勾选阶段 3，存在环境阻塞则保持阶段开放并记录阻塞项 |

### 6.1 B0 的诊断边界

- 先复现并区分 Electron 主进程失败、renderer `launch-failed`、GPU 子进程缺失依赖和系统驱动/资源故障；不要把 `app.disableHardwareAcceleration()` 当作默认修复。
- 使用随机隔离 `userData`，每次启动后只确认设置界面可用，不请求屏幕权限、不读取正式数据库。
- 每次失败都必须等待启动器完成子进程退出、流/sidecar 停止、隐私扫描和精确目录删除，再进行下一次尝试。
- 若同一机器连续 3 次冷启动通过，记录为启动稳定性门槛通过；单次偶然成功不足以关闭 B0。

### 6.2 B1/B2 的协作方式

- 前台规则验收以真实 Windows 前台探针为准。窗口控制工具可辅助打开应用，但不能替代用户在真实桌面上的最终切换证据。
- B1 只验证最容易浪费整场时间的 2:00/3:00 两个本地节点；3:00 未命中时立即结束，不继续等待 22 分钟。
- B1 通过后才在同一环境执行 B2；B2 中用户只负责选源、真实前台切换、停止共享、重新授权与暂停恢复，Agent 负责计时、证据和清理。
- 任一固定节点与预期不符时停止本场并读取 `processMatch`、`localVerdict`、`localReason`；不凭视觉印象补记通过。

### 6.3 排期优先级

1. 下一工作时段先完成 B0，不要求用户准备屏幕授权。
2. B0 通过后，单独预约约 40 分钟连续时间完成 B1 与 B2；若 B1 失败，当日不进入 B2。
3. B3 可在 B2 后同日执行，也可拆到下一时段；休眠恢复必须预留重新连接和清理时间。
4. B4 仅在用户同意和硬件满足时安排，不阻塞代码层面的诊断，但会阻塞阶段 3 的无条件“全部通过”声明。
5. B5 始终最后执行；其后才能讨论阶段 4 私有伙伴包生产。

## 7. 阶段 3 完成判据

只有 G3-A～G3-G 全部通过后才能同时执行以下动作：

1. 勾选 `docs/09-agent-handoff-plan.md` 的 S3-016、S3-017 和 S3-018。
2. 把 README 与路线图中的阶段 3 改为完成。
3. 运行 `pnpm run typecheck`、`pnpm test`、`pnpm run build`、真实 probe 专项和最终桌面清理/隐私扫描。
4. 提交一份不含截图、密钥、窗口标题或第三方 IP 的验收摘要。
5. 才允许进入阶段 4；安装包、签名和 ZIP 安装仍按 P1 独立排期。

## 8. 阶段 4 的启动顺序

阶段 3 关闭后，先验证多伙伴公共底座，再生产首个私有伙伴包：

1. 用两份原创伙伴夹具验证伙伴切换、场景、动作降级和关系进度隔离。
2. 在仓库外建立首个私有伙伴的视觉圣经与素材清单。
3. 生产标准关键帧、一个待机循环和十一条状态短片，并逐条执行身份、动作、媒体参数和字幕/TTS QA。
4. 使用公共 Schema 与安装期校验导入；若必须修改通用代码，先证明问题属于协议缺口而不是角色特例。
5. 连续使用一周，记录误判、打扰、媒体重复和信赖节奏，再决定阶段 5 调整项。

任何第三方角色图像、视频、字幕、台词、声音或制作提示词都不得进入仓库、安装包、测试夹具、日志或公开分享渠道。
