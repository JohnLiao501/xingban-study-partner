# AI Agent 项目执行规则

本文件适用于仓库根目录及其全部子目录。任何接手本项目的 AI Agent 在修改文件前，必须完整阅读本文件、`README.md` 与 `docs/09-agent-handoff-plan.md`。

## 1. 项目使命与当前边界

- 本项目是面向 Windows 的本地优先 AI 伴学应用。
- 底层统一使用 `Partner` 概念，不得把白厄或任何具体角色名、人格、场景、声音、动作写入通用代码。
- 白厄只是计划中的首个本机私有伙伴包。仓库、安装包、测试夹具和截图不得包含白厄或其他第三方 IP 素材。
- 当前主线是完成阶段 3：Windows 前台探针、本地规则自动判断、用户授权的低频单帧屏幕巡查，以及可选的兼容多模态 AI。
- 在阶段 3 全部门禁通过前，不开始阶段 4 的白厄私有伙伴包生产。
- MVP 不接入 Obsidian、摄像头、麦克风、自由语音对话、实时 3D、账号、云同步、好友榜或公开伙伴市场。

## 2. 开工前必读顺序

1. `README.md`：确认已实现范围、运行命令和文档入口。
2. `docs/09-agent-handoff-plan.md`：确认当前基线、已知差异、任务依赖和验收门禁。
3. `docs/01-product-requirements.md` 至 `docs/08-roadmap.md`：按需核对产品、隐私、数据和测试契约。
4. `shared/`、`electron/`、`src/`：以实际代码确认文档中的接口是否已落地。
5. `package.json`：只使用仓库实际存在的脚本，不臆造 lint、package 或 release 命令。

发现文档与实现不一致时，不得静默选择其中一方。先记录差异，再以产品隐私边界、已通过测试的行为和向后兼容为依据修正文档或实现，并在交付报告中说明。

## 3. 不可突破的产品与隐私规则

- 本地规则优先；只有规则无法判断、用户为本场显式授权屏幕捕获且开启 AI 时，才允许生成并发送单帧截图。
- 巡查截图只存在于内存，不落盘、不进入 SQLite、日志、崩溃报告、缓存、历史页或测试快照。
- 不发送视频、音频、摄像头、剪贴板、键盘记录、文件内容、聊天记录或多帧批次。
- 单帧最长边不超过 768 px，JPEG 质量为 60；处理完成后立即释放画布、缓冲区和流引用。
- `uncertain` 只能提醒，绝不消耗偏航次数。AI 的 `distracted` 必须由相隔 15 秒的两次独立单帧结果确认，且两次置信度均不低于 0.80。
- API 超时、429、5xx、网络失败、截图失败和响应无效一律安全降级为 `uncertain`，不得处罚用户。
- API 密钥除用户录入时短暂存在于设置表单并立即清空外，不得由主进程返回 renderer，也不得进入长期前端状态、SQLite、日志、错误对象或测试夹具；保存和短暂解密只在 Electron 主进程通过 `safeStorage` 完成。
- 原始窗口标题不持久化；结构化观察最多保存应用名与不可逆的窗口标题哈希。
- 伙伴包只能包含声明式 JSON 与媒体，不能控制 API 地址、读取截图、执行代码或发起网络请求。

## 4. 安全架构约束

- Electron 主进程是会话时间、巡查编排、数据库、秘密和系统能力的权威来源。
- renderer 视为不可信：保持 `contextIsolation: true`、`nodeIntegration: false`、`sandbox: true`。
- 所有 IPC 必须经受限 preload 暴露，并在主进程执行运行时白名单校验；不要把通用 `ipcRenderer`、文件系统或网络能力暴露给 renderer。
- 屏幕媒体权限只能授予专用隐藏截图窗口和一次明确的用户授权流程，禁止对任意 renderer 的 `media` 请求统一放行。
- 主督学室与巡查悬浮窗继续启用内容保护。阶段 3 必须用真实截图验证应用自身窗口不会进入巡查帧。
- 外部进程、sidecar 和兼容 API 的输出全部视为不可信输入：限制长度、严格解析、拒绝额外字段并设置超时。
- 数据库已有真实用户数据。新增字段或表必须追加新的迁移版本；不得修改已经执行过的 v1 迁移语义，也不得通过删除数据库解决迁移问题。

## 5. 实施方式

- 先确认工作区现场和用户已有修改，保留无关改动；本目录当前可能没有 Git 元数据，执行 Git 命令前先检查。
- 按 `docs/09-agent-handoff-plan.md` 的依赖顺序做小型纵向切片，不一次性混合前台探针、截图、AI、设置和 UI 的全部改动。
- 每个系统能力先定义共享类型与可替换接口，再实现 fake/mock，最后接入 Windows 或网络真实实现。
- 优先使用 Electron、Node 和 Web 平台内建能力。新增依赖必须说明必要性、许可证、包体积和安全影响；不要为方便而引入大型框架。
- Windows 前台探针最终实现必须可打包、可停止、可超时、可重启和可降级。若需要安装额外编译工具链，必须先取得用户许可。
- 测试不得访问真实外网、真实 API 密钥或用户屏幕；自动化使用 fake probe、合成帧和本地 mock server。
- 只有真实 Electron 手工验收才能使用用户主动选择的屏幕源，并在验收后停止全部捕获流和残留进程。
- 修改共享契约时，同步更新类型、运行时校验、preload、主进程 handler、测试和 `docs/06-data-and-ipc.md`。
- 修改用户可见行为或阶段状态时，同步更新 README、路线图和验收文档。

## 6. 每批次验证门禁

仓库当前没有 lint 脚本。每个实现批次至少运行：

```powershell
pnpm run typecheck
pnpm test
pnpm run build
```

涉及 renderer 时还必须进行浏览器预览的交互、窄屏和控制台检查；涉及 Electron、Windows 探针、屏幕捕获、托盘或 `safeStorage` 时还必须执行真实 `pnpm start` 冒烟测试。

真实 Electron 运行前必须先确认下列环境前提，否则会把环境问题误判为产品缺陷。以下三条根因都表现为 “GPU/renderer `launch-failed`”，完整分析见 `docs/11-stage-3-completion-plan.md` 第 6.4 节：

1. 子进程环境不得包含 `ELECTRON_RUN_AS_NODE`；一旦存在，Electron 会以纯 Node 运行，没有主进程、窗口和 GPU 进程，并抛出 `does not provide an export named '...'`。两个验收 runner 已通过 `buildChildEnvironment()` 自动剔除，手工运行请先 `env -u ELECTRON_RUN_AS_NODE`。
2. 必须在非沙箱、真实桌面会话中执行；沙箱会反复杀死 GPU 进程，导致所有 `loadURL` 返回 `ERR_FAILED (-2)`。
3. 不得以 `app.disableHardwareAcceleration()`、缩短连续判定时间或改动固定节点来换取表面通过。

真实 Electron 验收后必须独立确认：

- Electron 进程数量为 0；
- 没有指向本工作区的残留 Node/sidecar 进程；
- 屏幕流已停止；
- 应用数据目录、临时目录和日志中没有巡查截图或 base64 图像数据。

不得因为旧测试曾经通过就跳过当前批次验证。不得把“理论上可用”“代码已写”描述成“真实验收通过”。

## 7. 完成定义

一个任务只有同时满足以下条件才能标记完成：

- 功能从 UI/IPC 到主进程/存储形成闭环，或明确标记为纯接口/测试替身；
- 正常、取消、失败、超时和恢复路径均有处理；
- 隐私与安全规则有自动化测试，并按风险完成真实桌面验证；
- 类型检查、全部测试和生产构建通过；
- 相关文档与实际实现一致；
- 最终报告列出改动文件、验证命令、真实通过项、未验证项、已知风险和下一批次。

阶段 3 的完整任务顺序与逐项验收标准见 `docs/09-agent-handoff-plan.md`。若本文件与该计划发生冲突，以本文件的安全、隐私和 IP 边界为最高约束。
